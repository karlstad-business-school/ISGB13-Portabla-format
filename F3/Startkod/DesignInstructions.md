Instructions for AI Agent

You are working in a JavaScript project.

Your task is to update the existing script.js file inside Startkod.

Requirements

Inside the existing for-loop, generate the following DOM structure using JavaScript:

1. Create a container div
Create a <div> element
Add the following Tailwind classes:
bg-white p-6 rounded-lg shadow-md border border-gray-200 text-center
2. Create a title (h2)
Create an <h2> element
Add the following Tailwind classes:
text-xl font-bold mb-4 text-indigo-900
Set its textContent to the variable:
title
3. Create a content container
Create a <div> element
Set its innerHTML to the variable:
image
4. Style the image inside content
Use querySelector to select the <img> inside the content container
Add the following Tailwind classes to the image:
mx-auto mb-4 h-auto max-w-full rounded
5. Append elements
Append the <h2> and content <div> into the main container <div>
6. Add to DOM
Append the main container <div> into:
feedContainer
Notes
Do NOT create a new loop
Only insert this logic inside the existing loop
Use clean and readable JavaScript
Do not modify other parts of the file

End of instructions.